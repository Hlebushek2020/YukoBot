namespace YukoClientBase.Enums
{
    public enum RequestType
    {
        Authorization = 1,
        GetClientData = 2,
        UpdateServer = 4,
        UpdateServerList = 8,
        ExecuteScripts = 16,
        GetMessageCollections = 32,
        GetUrls = 64
    }
}
